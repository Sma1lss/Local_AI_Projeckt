# css

Каталог для стилей фронтенда.

## Содержимое

- `style.css` - общий stylesheet интерфейса.

## Состояние

Файл сейчас пустой, поэтому визуальный слой UI еще не реализован.

Запуск и подключение происходят через HTML-файлы родительской папки `UI_Web/`.
