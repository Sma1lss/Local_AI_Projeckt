# deploy/docker

Docker-артефакты Python AI-контура.

## Файл

- `python-service.Dockerfile` - базовый образ, из которого запускаются `retriever`, `router`, `executor`, `controller` и model servers через `docker-compose.yml`.
