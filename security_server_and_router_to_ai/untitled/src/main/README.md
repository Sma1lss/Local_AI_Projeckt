# src/main

Основной код и конфигурация Java-сервиса `fsss`.

## Что лежит внутри

- `java/com/fsss/` - пакеты бизнес-логики.
- `resources/` - `application.yml` и `logback-spring.xml`.

Профили `edge` и `sandbox` определяются конфигурацией в `resources/application.yml`.
