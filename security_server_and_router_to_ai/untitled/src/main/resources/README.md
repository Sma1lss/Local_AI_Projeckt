# resources

Конфигурация и логирование сервиса `fsss`.

## Файлы

- `application.yml` - базовый профиль, `edge`, `sandbox`, лимиты, sandbox-mode, scanner toggles.
- `logback-spring.xml` - console logger и rolling security log в `logs/security.log`.
